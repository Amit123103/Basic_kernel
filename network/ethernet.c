#include "network.h"
#include <stdint.h>

void ethernet_init(void)
{
    // Phase 12 stub: initialize Ethernet controller and MAC addressing.
}

int ethernet_send_frame(const void *frame, uint32_t length)
{
    (void)frame;
    (void)length;
    return 0;
}
